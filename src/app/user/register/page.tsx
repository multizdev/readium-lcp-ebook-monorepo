import React, { ReactElement } from 'react';

import RegisterPage from '@marketplace/auth/pages/RegisterPage';

function RegisterPageRoot(): ReactElement {
  return <RegisterPage />;
}

export default RegisterPageRoot;
