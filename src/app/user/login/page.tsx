import React, { ReactElement } from 'react';

import LoginPage from '@marketplace/auth/pages/LoginPage';

function LoginPageRoot(): ReactElement {
  return <LoginPage />;
}

export default LoginPageRoot;
